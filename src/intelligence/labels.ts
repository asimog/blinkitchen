/** Turn canonical ids into display labels: "indo_chinese" -> "Indo Chinese". */
export function humanizeId(value: string): string {
  return value
    .split(/[_-]/)
    .filter(Boolean)
    .map((part) => part.charAt(0).toUpperCase() + part.slice(1))
    .join(" ");
}
